name: 'Run Bundler Audit'
description: 'Runs Bundler Audit.'
author: 'Ben Crouse'
runs:
  using: 'docker'
  image: 'Dockerfile'
inputs:
  args:
    description: Arguments to pass to the bundler audit command
    required: false
